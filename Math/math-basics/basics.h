#ifndef BASICS_H
#define BASICS_H
#include <stddef.h>
#include <stdbool.h>
#include <stdlib.h>
#include <math.h>
#include <stdio.h>

unsigned int *find_multiples(unsigned int num, int limit);
unsigned int sum_of_squares(unsigned int number);
unsigned int square_of_sum(unsigned int number);

#endif